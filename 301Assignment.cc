#include <ctime>
#include <iostream>
#include <random>
#include <fstream>

using namespace std;

//Returns the median value in a given array A of n numbers. This is the kth element, where k = |n/2|, if the array was sorted.

void bruteForceMedian(int a[], int n)
{
    int k = abs(n/2);
    int numSmaller;
    int numEqual;

    for (int i = 0; i < n-1; i++)
    {
	
    }
}





int main()
{
    return 0;
}
